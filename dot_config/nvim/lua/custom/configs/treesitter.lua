local options = {
  ensure_installed = {
    "lua",
    "terraform",
    "hcl",
  },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
